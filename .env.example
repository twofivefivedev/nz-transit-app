# =============================================================================
# NZ Transit App - Environment Variables Template
# =============================================================================
# Copy this file to .env and fill in your values

# -----------------------------------------------------------------------------
# SUPABASE (PostgreSQL Database)
# -----------------------------------------------------------------------------

# Database URL (Pooler - Transaction Mode)
# Get from: Dashboard > Settings > Database > Connection string
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# Direct Database URL (for migrations)
DIRECT_DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# Supabase API URL
NEXT_PUBLIC_SUPABASE_URL=https://[project-ref].supabase.co

# Supabase Anon Key (safe for client-side)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase Service Role Key (server-side only, NEVER expose to client)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# -----------------------------------------------------------------------------
# UPSTASH REDIS (Real-time Data Cache)
# -----------------------------------------------------------------------------

UPSTASH_REDIS_REST_URL=https://[your-redis].upstash.io
UPSTASH_REDIS_REST_TOKEN=your-rest-token

# Metlink GTFS-RT API
METLINK_API_KEY=your-metlink-api-key

# Vercel Cron Secret (deprecated - kept for backwards compatibility)
# CRON_SECRET=your-cron-secret

# =============================================================================
# UPSTASH QSTASH (Scheduled GTFS-RT Data Sync)
# =============================================================================
# QStash is used for scheduling frequent GTFS-RT data syncs (every 30-60 seconds)
# instead of Vercel Cron which is limited to 1/day on free tier.
# Get keys from: https://console.upstash.com > QStash > API Keys

# Current signing key (used to verify incoming requests)
QSTASH_CURRENT_SIGNING_KEY=your-qstash-current-signing-key

# Next signing key (for key rotation, optional)
QSTASH_NEXT_SIGNING_KEY=your-qstash-next-signing-key
